apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: maven-run
spec:
  resources:
    inputs:
    - name: source
      resourceRef:
        name: maven-resource-petclinic
  taskRef:
    name: maven
