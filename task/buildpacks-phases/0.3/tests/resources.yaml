---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: buildpacks-pvc
  annotations:
    volume.kubernetes.io/selected-node: tekton-control-plane
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
